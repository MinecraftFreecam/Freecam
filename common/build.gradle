plugins {
    id "freecam.common-conventions"
}

loom {
    accessWidenerPath = file("src/main/resources/freecam.accesswidener")
}

configureBuildConfig {
    config = [
            cheatsRestricted: false,
            ignoreAllCollisionTooltipCount: 2,
            ignoreAllCollisionDefault: true,
            allowInteractTooltipCount: 2
    ]
}
