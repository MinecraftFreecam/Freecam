[project]
name = "freecam-ci"
version = "0.0.0"
description = "Scripts for Freecam CI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "json5 ~= 0.13.0",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
strict = true
testpaths = ["test"]
