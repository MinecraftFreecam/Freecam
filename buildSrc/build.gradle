plugins {
    id 'groovy-gradle-plugin'
}

gradlePlugin {
    plugins {
        overridePlugin {
            id = 'freecam.override'
            implementationClass = 'freecam.OverridePlugin'
        }
    }
}
